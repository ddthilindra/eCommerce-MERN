import { createTheme } from '@material-ui/core/styles';

const theme=createTheme({
    palette:{
        primary:{
            main:"#007acc"
        },
        secondary:{
            main:"#ffff"
        }
    },
    background:{
        default:"#ffff"
    }
})

export default theme;